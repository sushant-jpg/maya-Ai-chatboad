{
  "name": "maya-local-llm",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch server/server.js",
    "server": "node server/server.js",
    "test": "node --test tests/*.test.js",
    "build": "node --check api.js && node --check app.js && node --check chat.js && node --check ui.js && node --check server/app.js && node --check server/ollama.js && node --check server/server.js",
    "start": "node server/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
